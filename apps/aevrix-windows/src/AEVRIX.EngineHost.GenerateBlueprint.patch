--- a/src/AEVRIX.EngineHost/Program.cs
+++ b/src/AEVRIX.EngineHost/Program.cs
@@
-        case GenerateBlueprintCommand blueprint:
-            return ValidateBlueprint(blueprint);
+        case GenerateBlueprintCommand blueprint:
+            return await new BlueprintCommandHandler(AevrixDataPaths.ForCurrentUser())
+                .HandleAsync(blueprint);
@@
-static EngineResponse ValidateBlueprint(GenerateBlueprintCommand command)
-{
-    if (string.IsNullOrWhiteSpace(command.ProjectId) || !EngineProtocol.IsSafeCaptureId(command.CaptureId))
-    {
-        return new EngineResponse(command.RequestId, false, "invalid_blueprint", "Project and capture identifiers are required.");
-    }
-
-    return new EngineResponse(
-        command.RequestId,
-        false,
-        "blueprint_builder_pending",
-        "Canonical Blueprint export is implemented in AEVRIX.Core; evidence-to-blueprint synthesis wiring is the next engine layer.");
-}
